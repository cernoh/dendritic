# Upstream

- Source: `https://github.com/mattpocock/skills`, file `skills/engineering/to-spec/SKILL.md`.
- Commit: `74ca5fe077456a0b3b2f5310cf9430999fd0b5fd` (2026-09-17).
- Licence: MIT. The upstream notice is in `LICENSE-MIT.txt`.
- Local changes:
  1. `## Where the spec lives` replaces the tracker paragraph. The spec publishes as a GitHub issue with the `ready-for-agent` label.
  2. `## Source` is new. It names a cleared wayfinder map as the first source.
  3. One line renders the spec as a page with `render_html` when the user asks for it.
