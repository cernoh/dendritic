# Upstream

- Source: `https://github.com/mattpocock/skills`, file `skills/engineering/implement/SKILL.md`.
- Commit: `74ca5fe077456a0b3b2f5310cf9430999fd0b5fd` (2026-09-17).
- Licence: MIT. The upstream notice is in `LICENSE-MIT.txt`.
- Local changes:
  1. The `/tdd` reference becomes step 2: work at the seams the spec agreed, and prefer the highest seam.
  2. The `/code-review` reference becomes step 4: check the result against every acceptance criterion.
  3. Step 6 is new: comment the result on the ticket, close it, and refresh the map view with `wayfinder_view`.
  4. `tdd` and `code-review` are not vendored here, so the text names their work instead of the skills.
