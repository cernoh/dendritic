## What changed

Describe the change. One sentence per idea. Max 20 words per sentence.

## Why

State the reason for the change.

## Verification

State how you verified the change. Add commands and output.

## STE compliance

This text passes the STE linter (`.github/scripts/ste-lint.py`). The CI check fails when the title or the body has violations.

Run the linter on your draft:

```
python3 .github/scripts/ste-lint.py < your-draft.md
```

- [ ] I confirmed the linter passes on this text.
