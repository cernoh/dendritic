name: Nix quality

on:
  pull_request:
    paths:
      - '**/*.nix'
      - 'flake.lock'
      - '.github/workflows/**'
  push:
    branches: [main]
    paths:
      - '**/*.nix'
      - 'flake.lock'
      - '.github/workflows/**'

permissions:
  contents: read

jobs:
  nix-quality:
    name: Format Nix (changed files)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: DeterminateSystems/nix-installer-action@v16
      # Magic Nix Cache is sunset upstream; these are eval-only jobs, so no
      # binary-cache acceleration is wired up.
      - name: Find changed Nix files
        id: changed
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="origin/${{ github.event.repository.default_branch }}"
          elif [ "${{ github.event.before }}" = "0000000000000000000000000000000000000000" ]; then
            base="HEAD^"
          else
            base="${{ github.event.before }}"
          fi
          # Three-dot diff: compare against the merge-base with the default
          # branch, not its tip. Long-lived stacked branches otherwise see
          # files that exist only on main (e.g. .github/stubs from a PR
          # merged after this stack diverged) reported as changed — and
          # nixfmt --check crashes on paths missing from the checkout.
          files=$(git diff --name-only "$base"..."${{ github.sha }}" -- '*.nix' | tr '\n' ' ')
          echo "files=$files" >> "$GITHUB_OUTPUT"
      - name: Check formatting
        if: steps.changed.outputs.files != ''
        run: nix run nixpkgs#nixfmt-rfc-style -- --check ${{ steps.changed.outputs.files }}
      - name: No changed Nix files
        if: steps.changed.outputs.files == ''
        run: echo 'No changed Nix files; quality checks skipped.'
